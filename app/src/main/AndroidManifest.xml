<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.hacktrial">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".CardViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewsActivity" />
        <activity android:name=".NewsActivity1" />
        <activity android:name=".NewsActivity2" />
        <activity android:name=".NrewsActivity3" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".FirstActivity1" />
        <activity android:name=".FirstActivity2" />
        <activity android:name=".FirstActivity3" />
        <activity android:name=".AZActivity" />
        <activity android:name=".AZActivity1" />
        <activity android:name=".AZActivity2" />
        <activity android:name=".AZActivity3" />
        <activity android:name=".FoodActivity1" />
        <activity android:name=".FoodActivity2" />
        <activity android:name=".FoodActivity3" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".FitActivity" />
        <activity android:name=".FitActivity1" />
        <activity android:name=".FitActivity2" />
        <activity android:name=".FitActivity3" />
        <activity android:name=".FundActivity" />
        <activity android:name=".FundActivity1" />
        <activity android:name=".FundActivity2" />
        <activity android:name=".FundActivity3" />
        <activity android:name=".ChatActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".databaseactivity"></activity>
    </application>

</manifest>